Write a Python script `solution.py` that defines a function:

    def reverse_string(s: str) -> str:

which returns the input string reversed. The script must not print or
execute anything at import time - it should only define the function.

Respond with the complete file content in a single fenced Python code
block, nothing else.
