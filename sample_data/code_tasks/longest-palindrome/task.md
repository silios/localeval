## Longest Palindromic Substring

Given a string `s`, return the longest palindromic substring in `s`.

A palindrome reads the same forwards and backwards. If there are multiple
longest palindromes of the same length, return the first one encountered.

**Function signature:**
```python
def longest_palindrome(s: str) -> str:
```

**Examples:**
```
Input: s = "babad"    → "bab"  (or "aba", either is accepted)
Input: s = "cbbd"     → "bb"
Input: s = "a"        → "a"
Input: s = "ac"       → "a"  (or "c")
Input: s = "racecar"  → "racecar"
```
