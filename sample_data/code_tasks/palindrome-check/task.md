## Palindrome Check

Return True if the string is a palindrome (same forwards and backwards), ignoring case and non-alphanumeric chars.

**Function signature:**
```python
def is_palindrome(s: str) -> bool:
```
